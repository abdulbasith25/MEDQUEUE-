package com.appointment.dto.validation;

/**
 * Validation group marker interface for REGISTRATION requests.
 * Constraints tagged with this group are only enforced during /register.
 */
public interface OnRegister {}
