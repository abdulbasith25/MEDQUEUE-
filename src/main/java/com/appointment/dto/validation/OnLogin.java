package com.appointment.dto.validation;

/**
 * Validation group marker interface for LOGIN requests.
 * Constraints tagged with this group are only enforced during /login.
 */
public interface OnLogin {}
